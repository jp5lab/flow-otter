# FlowOtter Fix Plan — from the 2026-06-10 layout audit

> Committed copy of the audit fix plan (EVAL-7 ratification). The audit report it executes against is [`docs/audits/2026-06-10-layout-audit.md`](../audits/2026-06-10-layout-audit.md). Raw audit artifacts live in the local, gitignored `eval-results/2026-06-10-layout-audit/` directory; paths such as `tests/fixtures/audit-2026-06-10/` and `eval/replay/AUDIT-RERUN.md` are forward references created by later work items.

Synthesized from five workstream plans (renderer, staging, layout, lint/guidance, gates) and three critiques (safety, feasibility, gates). All blocker and major critique fixes are applied inline and marked **[Amended]**. Every disputed fact was re-verified at HEAD (`0648c57`); adjudications appear in §3 where they bind. Plan respects the safety spine (byte-identical idempotent compile, `_authoringKey` preservation, snapshot-before-deploy, drift refusal on `based_on_snapshot_hash`, consent gating), the full repo gate suite (typecheck, eslint, prettier, 777 unit + 17 property + 85 integration, build, tool-coverage, privacy-scan), npm install-weight discipline, and the Node-RED 4.0/4.1/5.0 support matrix.

---

## 1. Goal and definition of done

**Goal:** make FlowOtter's visual channel truthful, its staging batch-capable and lifecycle-complete, its layout machinery safe-then-good, its conventions taught in-band with numbers, and its claims mechanically falsifiable — converting the audit's NOT-YET (operator-readable layouts only at agent-as-layout-engine prices) into a verified pass.

**FULLY FIXED** is declared only when the audit re-run per `eval/replay/AUDIT-RERUN.md` (EVAL-6) passes ALL of:

| Criterion                | Bar                                                                             | Mechanically derived from                                                                                                                                                                                         |
| ------------------------ | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Friction                 | ≥4                                                                              | EVAL-1 budget account: all phase budgets met, 0 failed calls, 0 OOB mutations, 0 `force` — judge discretion ±0.5 only **[Amended: gates critique — friction anchored to the budget account, not judge sympathy]** |
| Cold-read + conventions  | ≥4 each, held                                                                   | Fresh non-author judges, audit rubric, original `e1-editor.png`/`e2-after-editor.png` committed as the 4.5/4 anchors                                                                                              |
| S5 see-judge-adjust loop | ≤6 **total** invocations (MCP + Read/exec), ±2px live-editor fidelity           | `npm run eval:s5`, twice consecutive, plus one live unscripted session                                                                                                                                            |
| S6 layout benchmark      | Pre-registered thresholds pass, **both legs** (stripped + zero-coordinate spec) | `eval:s6 --scored` against sha256-frozen `thresholds.json`                                                                                                                                                        |
| e2 replay                | Pure-MCP ≤5 calls + 1 confirmation, 0 failed, 0 OOB, wiring byte-identical      | `eval:replay --scenario e2 --phase 1`                                                                                                                                                                             |
| Repo gates               | All green                                                                       | typecheck/eslint/prettier/777u/17p/85i/build/tool-coverage/privacy-scan                                                                                                                                           |
| Safety spine             | Zero regressions                                                                | `eval:canary` (S4 drift-refusal/rollback/decline drills + S1 idempotency) after every fix batch; twice-consecutive rule for every gate declaration                                                                |

The e1-phase2 leg and all S6 leg-B specs are **mechanically asserted position-free** — a hand-placed spec disqualifies the run **[Amended: gates blocker — closes the agent-as-its-own-layout-engine gaming path]**.

---

## 2. Phase plan

Global `_stage-pipeline.ts` merge order (binding across all phases): **WSB-3 → WSB-5-PR1 (extraction + async-op widening) → REND-8 (inside `compileValidateAndStage`) → D-3 → LAYO-6**. Each later PR re-runs the earlier items' invariant pins (single-slot refusal, `based_on_snapshot_hash` = single-load runtime baseline, `staged_hash` byte-identity with/without enrichment). REND-8's "only item touching this file" claim is struck; the async-op widening is owned by WSB-5-PR1 and deleted from LAYO-6's scope. **[Amended: safety major — five-way pileup resolved; D-3↔REND-8 order swapped vs the critique to match phasing, functionally order-free since both sit inside the extracted tail.]**

Five cross-stream contracts frozen in week 1:

1. `renderGeometry(flows, tabId)` exported from `src/toolkit/render` returning per-node `{id, x, y, w, h, ports[]}` (center-convention, post-translate) — the single geometry source for the fidelity comparator and EVAL-4 blind packs. `render_flow_png` additionally accepts `include_geometry: boolean` (default false) emitting the same array. **[Amended: resolves the A↔E geometry blocker in all three critiques]**
2. `GeometryProvider` (dims **w and h** + port anchors) implemented once in `src/toolkit/render/metrics.ts`, consumed by D-1 lint, D-4 placement, LAYO-4 engine. **[Amended: feasibility major — no re-derived formulas]**
3. `src/toolkit/lanes.ts` — ONE module: `Lane`, `LANE_NAMES`, `LANE_ORDER = ['main','indicate','error']`, `LANE_GAP = 120`, one error-closure algorithm with `TabSpec` and `FlowsJson` adapters. **[Amended: feasibility blocker — kills the LAYO-3/D-2 fork; gates minor — taught 120px and engine gap are the same exported constant]**
4. `stage_changes` op vocabulary (`op-schemas.ts`, move op normalized to `tab_id`) ↔ EVAL-5 e2 steps file, landed in the same PR series.
5. `layout_lint` weighted [0,1] per-rule score schema ↔ EVAL-3 thresholds.

### Phase 1 — Faithful eyes + honest errors → **v1.4.0**

- **Theme:** the tool's visual channel stops lying; errors carry their diagnostics; cheap staging frictions die; conventions get numbers.
- **Items:** EVAL-7 (lands first, ratifies this plan), EVAL-1, WSB-1, WSB-3, WSB-6, WSB-5-PR1 (pure refactor), REND-1 → REND-2 → REND-3 → REND-4 → REND-5 → REND-8, REND-7, D-5, EVAL-2, EVAL-6 (canary steps + AUDIT-RERUN anchors).
- **Dependency rationale:** R3 orders "fix the renderer before delivering it" — `render_flow_png` (REND-5) gates on geometry fixes (REND-2/3) which gate on the editor ground-truth fixture (REND-1). REND-8 is in this phase because the honest ≤6-invocation S5 loop is only achievable via stage-output before/after renders — verified: an explicit-render loop with the mandatory discard costs 7 (single-slot guard confirmed at `_stage-pipeline.ts:72-82`). **[Amended: gates blocker overrides the feasibility trim for REND-8; WSB-5-PR1 rides along because REND-8 must live inside `compileValidateAndStage`.]** This phase is heavy for a solo maintainer; REND-7's live-check half and EVAL-6's AUDIT-RERUN.md are designated trailers (may land early in Phase 2 without blocking the release; the canary steps files and the static fidelity floor may not).
- **Exit gate:** `eval:s5` green **twice consecutively** (≤6 total invocations + ±2px fidelity via `renderGeometry`) + `eval:canary` green + **one live unscripted Claude Code session** whose transcript shows the stage→see→adjust→re-see loop within budget, recorded in the run file. **[Amended: gates major — restores DESIGN.md Phase-0 item 4 (live spike) instead of superseding it]**
- **Re-runs:** `eval:canary` (S4+S1) after every fix batch; full repo gates per PR.

### Phase 2 — Batch staging, full lifecycle, spatial lint → **v1.5.0**

- **Theme:** one stage / one deploy / one confirmation for arbitrary reorganizations; comments/groups/junctions get full lifecycles; probes stop being spatially blind.
- **Items:** WSB-2 → WSB-4 → WSB-5 (PR-2/3), WSB-7 (new), WSB-8 (new), D-1 (merged with REND-6), D-2, D-4, D-3, LAYO-1, EVAL-3, EVAL-5.
- **Dependency rationale:** WSB-2 (deriveId two-pass) lands before any lifecycle/batch work so new ops build on sound id semantics. WSB-4's op union is a hard prerequisite of WSB-5's discriminated union. D-1 follows REND-2 (Phase 1) so the GeometryProvider is editor-true from day one. LAYO-1 is dependency-free and kills the active ELK crash. EVAL-3's pre-registration must merge **before any scored S6 run** (Phase 3) — placed here so thresholds freeze before the engine exists. EVAL-5's e2 steps + safety post-conditions merge **before or with** WSB-5's first staging PR (test-first house rule); `eval:canary` runs per-PR (not per-batch) for all WSB items.
- **Exit gate:** `eval:replay --scenario e2 --phase 1` (≤5 MCP + 1 confirmation, 0 failed, 0 OOB, wiring-map byte-identity, sibling tab untouched) + e1 phase-1 ceiling (≤30 MCP / ≤3 confirmations) + canary twice + **one live S7 cold-agent run** stating all eight layout criteria from MCP channels alone (instructions/catalog/prompts — D-5's channels; D-7's static capstone follows in Phase 3). **[Amended: gates major — live S7 binding]**
- **Re-runs:** canary per WSB PR; elevated `VITEST_PROP_RUNS` for WSB-2 and WSB-5 before merge.

### Phase 3 — Layout engine, flagship, scored benchmark → **v2.0.0**

- **Theme:** the machine becomes its own layout engine — opt-in, proven before exposure; declarative `stage_spec` ships only with real placement (fixes the audit report §5.6, `docs/audits/2026-06-10-layout-audit.md`).
- **Items:** LAYO-2 (descoped) ∥ LAYO-3 → LAYO-4 (epic, kill-switch) → LAYO-5 → LAYO-6, D-6, D-7, EVAL-4 (scored mode); plus DESIGN-D1 `stage_spec` (specced in DESIGN.md, deliberately sequenced here, not in this catalog) and D7 surface consolidation riding the major.
- **Dependency rationale:** LAYO-4 consumes Phase-1's editor-true metrics through the `GeometryProvider` seam and Phase-2's shared lanes module; LAYO-5 acceptance precedes LAYO-6 exposure (D4 gate: `layout` toolset default-OFF until S6 passes); D-7's numeric pins calibrate once, against the final editor-true provider **[Amended: safety minor — D-7 gains depends_on REND-2]**. LAYO-4 carries a **kill-switch decision point after slice A**: if compound+ports output on the e1 fixture already misses "hand-arranged" on visual inspection, stop before slices B/C and keep layout opt-in. **[Amended: feasibility major]**
- **Exit gate:** `eval:s6 --scored` pass on **both legs** against sha256-frozen thresholds + `eval:replay e1 --phase 2` (≤3 authoring + 1 confirmation, zero-coordinate spec mechanically enforced) + S2/S3 green with auto-layout + canary twice.
- **Re-runs:** canary per batch; renderer fidelity check re-run on any Node-RED minor bump.

### Phase 4 — FULLY FIXED declaration (no release)

- **Theme:** audit re-run with fresh judges per `eval/replay/AUDIT-RERUN.md` using the §1 mechanical anchors. Only after passing: DESIGN-D10 skills repo, FlowFuse side-by-side, marketing.

---

## 3. Work items

**Fact adjudications (critic vs planner, re-verified at HEAD):**

- _Safety/feasibility/gates critics right:_ `flows-lint.ts` is one 109-line function — REND-6 and D-1 could not both land (fixed 120×40 top-left boxes, off-canvas as severity `error` at the point not the box, group/comment skip — all confirmed). `move_node` takes `source_tab_id` (`move-node.ts:17`) — unowned outlier, confirmed. `add_node` resolves `source_node_id` via `resolveAuthoringKey(priorFlows, …)` (`add-node.ts:142-147`) — WSB-5's intra-batch gap is real. `deriveId`'s exact-match-first chain (`compile.ts:160-178`) means a batch remove-then-readd of the same `tab:kind:key` **inherits the prior id** while the sequential path mints fresh — WSB-5's equivalence property was self-contradictory as written. `explain.ts:98` skips non-regular nodes in the edge walk — SD7 confirmed unowned. e2spag001 has switch `e2n05` (outputs:2/rules:2) and **zero** catch/status/complete nodes — LAYO-5 was missing one assertion and silently dropping an abstention. `EVALUATION.md:120-122` falsely claims `files` ships `docs/` (actual: `[dist, README.md, CHANGELOG.md, LICENSE]`). `SERVER_INSTRUCTIONS` is exactly 1902/2000 chars. No docs-assertion test exists (WSB-6's conditional resolves to: add one). Single-slot stage-over-stage refusal already exists at HEAD — EVAL-7's phase map mischaracterized it. Build is `rm dist && tsc` — REND-5's font bundling had no packaging path.
- _Feasibility critic right on a size fact:_ `puppeteer-core` is ~5-8MB and downloads no browser — EVAL-2's "~100-300MB" conflated it with full puppeteer. Decision still goes to the zero-new-dep CDP route (below), with puppeteer-core recorded as the documented fallback.
- _Planners' corrections to the audit all held:_ `nodeWidthFor` has a `hasIcon` param (defect is call sites hardcoding `false`); the bbox lint lives in `flows-lint.ts` not `validate/index.ts`; `FLOWOTTER_SESSION_ID` exists undocumented at `container.ts:226`; `ws` is already a runtime dependency.

### Workstream A — Renderer (FAITHFUL EYES, R3)

**REND-1 — Editor ground-truth metrics capture (resolves DESIGN.md open question 3)** — Size M, depends: none.
Files: NEW `scripts/eval/cdp.mjs` (shared CDP module), NEW `scripts/editor-metrics-dump.mjs`, NEW `tests/fixtures/editor-metrics/nodered-4.1.11.json` + `nodered-5.0.0.json`, NEW `tests/fixtures/render/calibration-flow.json`, `docs/EVALUATION.md` (recipe).
Sketch: calibration flow with a 0–40-char label ladder, one of each core node type (inject/debug buttons, switch with 1/2/4 rules, link l:true/false, catch/status/complete), comments, a wired junction, a 2-node group. POST to sterile stack, `chrome --headless=new --remote-debugging-port`, connect over CDP **via the shared `scripts/eval/cdp.mjs` module built on the existing `ws` runtime dep — this module is the single browser-automation stack for REND-1, REND-7, and EVAL-2; no playwright/puppeteer dependency is added** **[Amended: safety+gates majors — three capture scripts unified; feasibility's puppeteer-core stays the documented fallback if raw CDP proves brittle]**. Dump `RED.nodes` model geometry `{type,name,x,y,w,h,outputs,inputs}`, junction/comment/group DOM bboxes, label `getComputedStyle`, and per-port-count output-port y-offsets. Versioned fixtures for 4.1.11 and 5.0.0; **record the explicit assumption that 4.0.x dims equal 4.1 (the appearance rework was 5.0), with an optional 4.0 capture leg if a container is handy** **[Amended: feasibility major]**. Capture is one-time and committed; CI never runs it. Safety impact: none.
Tests: `editor-metrics-fixture.test.ts` — schema sanity, empirical invariants (w ≥ 100, mod-20), cross-version drift test that fails loudly with a table if 4.1 vs 5.0 dims differ.

**REND-2 — Editor-true node dimension model + compile auto-fit consumer (F11, F9)** — Size M, depends: REND-1. **Safety-invariant: test-first.**
Files: `src/toolkit/render/metrics.ts` (`nodeWidthFor:58-65`, constants), `glyph-widths.ts:118` (12→14px), `src/toolkit/authoring/compile.ts:278-340` (autoFit, `:318` hardcoded `(label,false,1)`), `tests/unit/toolkit/authoring/group-autofit.test.ts`, NEW `metrics-editor-truth.test.ts`.
Sketch: `nodeDimensionsFor(label, {hasIcon, inputs, outputs, hasButton}): {w,h}` — `w = max(100, 20·ceil((labelPx@14px + icon + handle + pad)/20))`, MIN 80→100, 240 cap removed unless fixture shows one; `h = max(30, outputs·15)` per fixture. Per-type icon/button override map seeded from fixture. `nodeWidthFor` stays as a thin wrapper during migration; both call sites updated. This module also exposes the **GeometryProvider** (dims + port anchors) — frozen contract #2. **Compile/auto-fit pins ONE dimension profile (4.1) permanently regardless of target runtime — version-keyed profiles, if the REND-1 diff test fires, apply only to render and lint paths which have ctx access; `compile()` stays pure** **[Amended: feasibility major — version-keyed-metrics-vs-pure-compile ruled now]**. Safety: compile output changes only for autoFit-path groups; `decompile.ts:324-327` preserves explicit geometry — pinned by a decompile→compile byte-identity regression on the canonical e1 fixture including `_authoringKey` ids.
Tests: table-driven ±2px vs the 4.1.11 fixture; audit's named cases exact ('Parse reading' → 160 not 107; 'Debounce repeat alarms' → 220 not 163); properties (monotonic, ≥100, ≡0 mod 20, deterministic); deliberate group-autofit re-bless with review note; byte-identity roundtrip.

**REND-3 — Renderer geometry correctness (F2, F3, F6, e2#11, e1#9 render-side)** — Size M, depends: REND-2.
Files: `src/toolkit/render/svg.ts` (boxes 175-210, ports 191-194, wires 212-237, extents 239-254, draw 256-286, `outputPortY:303-305`), `svg.test.ts`, all 5 blessed snapshots re-blessed, canonical audit fixture (see EVAL-3 note on the single fixture dir).
Sketch: (1) center anchors for nodes/comments (groups stay top-left — flows.json group x/y/w/h is a bbox); ports at editor positions pinned by fixture. (2) Port counts via `getOutputPortCount(n.type, n as Record<string,unknown>)` — kills the phantom `passthrough` read; subflow instances from def `out` length. (3) Junctions render as r≈5 waypoint circles, participate in the wire walk via `wires[0]`. (4) Config-by-reference nodes excluded (referenced from another node's scalar string props, excluding wires/links/scope/g/z/d/id; belt-and-braces `isConfigNode` shape check) — _renderer-side workaround; the root cause is WSB-8_. (5) Whole-body translate so negative extents are never clipped. (6) Heights from `nodeDimensionsFor`. Exports **`renderGeometry(flows, tabId)`** (frozen contract #1). Safety: read-only; determinism property must stay green.
Tests: re-bless all 5 snapshots in the same PR **with the re-bless protocol** (a snapshot may only be re-blessed alongside an assertion test naming the geometry it changes); assertions that survive re-blessing: (a) e1 switch renders exactly 2 output ports, (b) no `<rect>` for the mqtt-broker id, (c) junction is `<circle>` with outgoing wire path, (d) group containment, (e) node at (0,0) fully visible, (f) wire endpoints equal port coordinates. `read-tools.test.ts:128` updated.

**REND-4 — `against:'staged'|'runtime'` on render_flow_svg (F7)** — Size S, depends: none.
Files: `src/server/tools/read/render-flow-svg.ts`, tests, `docs/TOOL_REFERENCE.md`.
Sketch: `against` enum defaulting `'runtime'`; staged path reads `ctx.staging.read()`, throws `ValidationFailedError` with diagnostics when slot empty; output gains `against`, `staged_hash` (snake_case per WSB-6), nullable `based_on_snapshot_hash`; prompts/recipes at `registry.ts:105,138,157` get one-line truth edits. Mirrored on `render_flow_png`. Safety: read-only.
Tests: staged render contains the staged node, runtime render does not; empty-slot error with diagnostics; default back-compat.

**REND-5 — NEW tool `render_flow_png` (F1, D5, R3)** — Size M, depends: REND-3, REND-4.
Files: NEW `src/server/tools/read/render-flow-png.ts`, NEW `src/toolkit/render/png.ts`, `_tool.ts` (`buildContent` hook), `register.ts`, `stdio.ts:83-106`, `index.ts` (ALL_TOOLS; **no SERVER_INSTRUCTIONS edit — its one-line mention is handed to D-5**), `toolsets.ts:77`, `container.ts` (RENDER_DIR), `package.json` (optionalDependencies), docs (kill the qlmanage recipe).
Sketch: `@resvg/resvg-js` as optionalDependency (~2MB per-platform N-API prebuilds); dynamic import; on failure throw `RasterizerUnavailableError` with install hint — **HARD-FAIL, never silent SVG substitution**; `health_check` gains `rasterizer_available` as an explicit deliverable. **Font: bundle an OFL sans subset as a base64-embedded generated TS module (tsc-friendly, no build changes; ~200KB tarball delta) → byte-stable golden PNGs** **[Amended: feasibility major — packaging mechanism specified; the `rm dist && tsc` build copies no assets]**. Input `{tab_id, against, output_path?, scale? (≤4), include_geometry? (default false), return_image? (default false)}` — **image content block is opt-in; Claude Code reads `png_path` from disk and base64 blocks are dead weight there** **[Amended: feasibility minor]**; `include_geometry` emits the `renderGeometry` array **[Amended: contract #1]**. Atomic Buffer `safeWrite`; output `{png_path, width_px, height_px, rev, staged_hash, against, geometry?}`. `buildContent` plumbing through stdio with a default-path byte-identity regression. Safety: read-only tool; stdio change additive and pinned.
Tests: PNG magic bytes + dimensions; mocked-import `RasterizerUnavailableError` (pins no-silent-degradation); staged variant; `include_geometry` matches `renderGeometry` byte-for-byte; stdio default-path regression; integration e2e satisfying tool-coverage; golden PNG of the e1 fixture (bundled font makes it unconditional).

**REND-6 — MERGED into D-1.** Geometry lint ownership is single: D-1 (Workstream D). REND-6's deliverables that survive: `nodeDimensionsFor` feeding D-1's default GeometryProvider (already in REND-2), and its named regression cases moved into D-1's test files (e3 planted-defect pair; wide-nodes-140px-apart caught / narrow-nodes-110px cleared; group-half-off-canvas; previously-staging-clean fixture still stages). **[Amended: blocker in all three critiques — dual ownership of `flows-lint.ts` eliminated]**

**REND-7 — Renderer-fidelity regression harness (R3 acceptance, F9)** — Size M, depends: REND-1, REND-3.
Files: NEW `scripts/editor-fidelity-check.mjs` (on `scripts/eval/cdp.mjs`), NEW env-gated `tests/integration/editor-fidelity.test.ts`, docs.
Sketch: layer A (CI): REND-3's assertion tests + the re-bless protocol documented in the test header. Layer B (periodic/eval): import the canonical e1 fixture into the sterile stack, CDP-dump per-node geometry + ports, compare against `renderGeometry` output with the **single ±2px comparator (per-corner + per-port — the stricter basis), exposed as a library that `npm run fidelity:editor` AND `eval:s5` both invoke; EVAL-2's duplicate comparator is deleted** **[Amended: gates minor + safety major]**. Env-gated (`FLOWOTTER_LIVE_EDITOR`) so the 85-integration suite stays green. Wired into EVALUATION.md as an S5 prerequisite and per-Node-RED-minor checklist item.
Tests: comparator unit tests (synthetic deltas → pass/fail/report); fixture-freshness guard (`nodeRedVersion`/`capturedAt` vs live editor); R3 acceptance rehearsal on the e1 fixture.

**REND-8 — Before/after render paths on stage outputs (D5 second half)** — Size S, depends: REND-5, WSB-5-PR1. **Safety-invariant: test-first.**
Files: `_stage-pipeline.ts` — **implemented INSIDE the extracted `compileValidateAndStage` so per-op tools AND `stage_changes` batches both emit before/after renders** **[Amended: safety major]** — `get-staged-change.ts`, stage-output shape tests, `docs/ARCHITECTURE.md`.
Sketch: post-`staging.write`, render prior and staged flows for touched tabs: SVG always, PNG when the rasterizer imports; `StageBase.render = {before_svg, after_svg, before_png|null, after_png|null, rasterizer_available}` — absence LOUD, never silent substitution. Render failures never fail the stage (try/catch → `render:null`). Sits in the published merge order; ships as its own reviewed PR. Safety: zero change to compile/validate/lint/stage logic, hashes, drift refusal, single-slot error; enrichment strictly after write, output-only — pinned by hash byte-identity with/without enrichment.
Tests: shape test (before≠after on a node-add); rasterizer-absent path (stage still succeeds, `rasterizer_available:false`); render-failure injection; hash invariance; existing single-slot tests unchanged.

### Workstream B — Staging & lifecycle (R5)

**WSB-1 — Serialize structured error payloads through stdio (SD2)** — Size S, depends: none.
Files: `stdio.ts:83-106` catch block, NEW `src/server/transport/tool-error.ts`, `_tool.ts:130-145`, `adapters/nodered/errors.ts:65-74`, NEW `tool-error.test.ts`, integration.
Sketch: pure `toolErrorContent(toolName, err)` keeping the human-readable first line + a JSON block `{error: {name, message, diagnostics?, expected_hash?, actual_hash?, failed_op_index?, failed_op?}}`; ValidationFailedError → diagnostics verbatim; DriftError → hashes; BatchOpError (WSB-5) → op index; cap 50 diagnostics with truncation marker. Safety: error path only; success path byte-identical (pinned).
Tests: per-branch payload assertions; integration regression pinning the e2 defect ("1 validation error(s)" never again without a cause); success-path snapshot.

**WSB-2 — Fix cross-tab deriveId id-steal (SD1): two-pass derivation + collision-guarded fresh ids + exposed `key` on add_group/add_comment** — Size M, depends: none. **Safety-invariant: test-first (ID-preservation core).**
Files: `compile.ts:86-178, 468-509, 645-661`, `add-group.ts`/`add-comment.ts` InputSchemas + handlers, `tests/property/arbitraries.ts`, `compile-cross-tab-move.test.ts`, NEW `compile-cross-tab-id-steal.test.ts`, integration.
Sketch (root cause verified at HEAD: tab-order resolution lets a new same-`kind:key` group on a sort-earlier tab steal the owner's id via the `byKindKey` fallback; the owner's fresh path regenerates the SAME id because `generateNodeId(seed)` is deterministic and never consults `reservedIds`): precompute entry list; **Pass A** reserves all exact `tab:kind:key` matches; **Pass B** (same order) byKindKey → legacy id → fixed → fresh with salt loop `generateNodeId(seed + '~' + n)` until unreserved. Surface: `key` optional on add_group/add_comment (op support already exists). Safety: Pass A guarantees idempotence by construction; cross-tab moves still resolve via Pass B; salt loop deterministic.
Tests (written first): deterministic e2 repro (tabA group key stolen by sort-earlier tabB; no duplicate ids; owner keeps exact id) + comment mirror + order-independence; arbitraries extended to generate duplicate kind+key on new tabs; regression pins: compile-idempotent, cross-tab-move, round-trip, spec-example-idempotent, multi-edit-id-preservation; integration: the e2 8/8-refusal sequence now succeeds. Elevated `VITEST_PROP_RUNS` before merge.

**WSB-3 — Stage-time no-op refusal + auto-clear hash-equal stale stages (SD3 pipeline half, SD6 half)** — Size S, depends: none. **Safety-invariant: spine-adjacent.**
Files: `_stage-pipeline.ts:69-114`, tool OutputSchemas, `stage-overwrite-guard.test.ts`, NEW `stage-noop-guard.test.ts`, integration (drift-detection must stay green).
Sketch: (1) AUTO-CLEAR — hoist the single `flowSource.load()` above the pending check; when `pending.stagedHash === canonicalHash(priorFlows)` → clear + info diagnostic `staging/auto-cleared-stale-stage`, regardless of agent_id (byte-equality = information-lossless); otherwise refuse with a message that now names `force_takeover`. (2) NO-OP REFUSAL — post-compile, `compiled.hash === priorHash` → `ValidationFailedError` ("produced no change… check kind: node vs junction vs comment vs group"), nothing written. **WSB-3 does NOT touch `operations/*.ts` return shapes — the silent-false→throwing op-contract flip is wholly owned by WSB-4** **[Amended: safety minor — single owner]**. Safety: drift check untouched; auto-clear can never mask drift (fires only on byte-equality); refusal strictly tightens `REQUIRE_DIFF_BEFORE_DEPLOY` to stage time, killing the e1 79-call poison cascade.
Tests (first): remove-on-comment-key refused AND slot stays empty; junction update refused; value-identical update refused; foreign-agent hash-equal stage cleared (pins the e2 restart friction); hash-unequal still blocked; drift/deploy/rollback suites untouched.

**WSB-4 — Full object lifecycle (SD3, SD4): junction/comment addressing in move/update/remove_node; new update_group, remove_group, update_comment; group refit; minimal add_junction** — Size L, depends: WSB-3.
Files: `operations/move-node.ts`, `remove-node.ts`, `update-node.ts`, NEW `operations/{update,remove}-comment.ts`, `{update,remove}-group.ts`, **NEW `operations/add-junction.ts`** **[Amended: open question resolved by default — the batch union and the layout engine both need it]**, three new author tools + ALL_TOOLS/toolsets/TOOL_REFERENCE, op unit tests, integration.
Sketch: ops become **pure and throwing — no silent-false returns (this item owns the contract flip and the migration of the two existing silent-false op tests, `remove-node.test.ts:41` / `update-node.test.ts:53`)**. moveNode lookup extends nodes → junctions → comments; removeNode scrubs junction connections and comment group membership; updateNode maps junction fields and rejects passthrough-on-junction; `updateGroup` with `refit:true` strips position+size so compile's `autoFitGroupGeometry` recomputes (explicit opt-in — byte-stable recompiles preserved); `removeGroup` = Node-RED ungroup (members NEVER deleted, children reparented). Safety: all ops edit by key, never rename keys → Pass-A exact match preserves every surviving id; refit is a pure function of member positions.
Tests: per-op suites (cross-tab junction move with connections; comment scrub; refit equals autoFit of post-move positions; ungroup releases members + reparents + ids unchanged; every not-found throws); round-trip/idempotent properties stay green; integration: the e2 lifecycle sequence pure-MCP. Splits into two PRs (addressing; group tools).

**WSB-5 — `stage_changes` atomic batch (R5 acceptance vehicle, F5)** — Size L, depends: WSB-1..4, REND-4. **Safety-invariant: test-first on the drift contract.**
Files: NEW `stage-changes.ts`, NEW `op-schemas.ts`, NEW `operations/batch.ts`, `_stage-pipeline.ts` (PR-1 extraction — Phase 1), `_tool.ts` (BatchOpError), index/toolsets/docs, NEW unit + property + integration suites incl. `e2-reorg-budget.test.ts`.
Sketch: PR-1 (Phase 1): extract `compileValidateAndStage(ctx, prior, nextSpec, meta)` **and perform the async-op widening (deleted from LAYO-6)**. PR-2/3 (Phase 2): tool with `{ops: Op[] (1..200), reason?, dry_run?}`, discriminated union over per-op schemas extracted to `op-schemas.ts` — **the move member is normalized to `tab_id` (+ `dest_tab_id`) before the union ships** **[Amended: safety major — don't bake the outlier into new flagship surface]**. Execution: single load → single decompile → fold ops left-to-right → ONE compile(prior) → ONE validate/lint/diff → ONE staging.write with `basedOnSnapshotHash` = batch-start runtime hash. **Reference resolution table (per field — `source_node_id`, `node_id`, `from_key`/`to_key`, `node_keys`, `group_key`, scope/pinned keys): resolve first against keys in the evolving folded spec, then against priorFlows ids; this also lands e1#10 (authoring keys accepted where runtime ids were required)** **[Amended: safety blocker + feasibility major]**. **Within-batch remove-then-readd of the same `tab:kind:key`: `applyOps` maintains a tombstone set of removed `(tabId, kind, key)` tuples passed to compile so re-added keys mint fresh ids — preserving the batch ≡ sequential equivalence property; the tombstone behavior gets its own dedicated unit test** **[Amended: safety blocker — verified the inherit-id mechanism at `compile.ts:160-163`]**. All-or-nothing via BatchOpError (slot untouched); post-compile lint errors abort whole batch with full diagnostics through WSB-1. WSB-3's pending guard runs identically at batch start. Output includes `op_results` and a documented extension point for REND-8 render paths.
Tests (drift suite first): baseline = pre-batch runtime hash; OOB mutation between stage and deploy → DriftError; pending non-equal stage → blocked; snapshot + rollback byte-identical. Unit: ordering (add key 'k1' then wire from 'k1'); same-batch add referenced by later op AND a pre-existing runtime id in one batch; tombstone fresh-id; all-or-nothing with `failed_op_index`; dry_run leaves slot empty; **net-zero batch (move then move back) refused by WSB-3's no-op guard with slot empty** **[Amended: safety minor]**. Property: batch ≡ sequential (with tombstones the class is now covered, not excluded); identical re-stage → identical `staged_hash`. Acceptance: e2 reorganization as ONE call, ≤5 MCP + 1 confirmation, **counting boundary cited from EVAL-1's glossary and mirrored in the test structure** **[Amended: gates minor]**; sabotage variant names failing op index + rule. Elevated `VITEST_PROP_RUNS`.

**WSB-6 — Casing/ownership/vocabulary reconciliation (SD6)** — Size S, depends: none.
Files: `get-staged-change.ts:8-43`, `move-node.ts` schema, NEW `src/server/nudges/rules/param-vocabulary.ts` (or extend existing nudge), `docs/CLIENT_CONFIG.md`/`TOOL_REFERENCE.md`/`AGENT_QUICKSTART.md`, CHANGELOG, NEW `tests/unit/docs/client-config-coverage.test.ts`.
Sketch: `get_staged_change` emits snake_case canonical (`staged_hash`, `based_on_snapshot_hash`, …) plus `agent_id`, `owned_by_current_session`, `stale`; dual-emit legacy camelCase for one minor, removal slated v2.0.0 with recorded supersession. **move_node gains `tab_id` as an accepted alias with `source_tab_id` deprecated in the description, plus the param-mapping nudge (owns e3#2)** **[Amended: safety major]**. Docs: `FLOWOTTER_SESSION_ID` (verified existing-but-undocumented at `container.ts:226`) gets a CLIENT_CONFIG "Staging ownership" section; ownership model + WSB-3 auto-clear documented. Safety: read-surface + docs only.
Tests: both casings during the window; agent_id/ownership/stale correctness; staged_hash feeds deploy without renaming; alias additive (existing integration green); **docs-presence pinned by the new unit test asserting CLIENT_CONFIG.md contains `FLOWOTTER_SESSION_ID` — the "CHANGELOG review" fallback is deleted** **[Amended: gates minor — no docs test existed; one is added]**.

**WSB-7 (NEW) — explain_flow walks junction edges (SD7)** — Size S, depends: none. **[Amended: gates blocker — the single unpinned row in the coverage matrix; defect verified at `explain.ts:98`]**
Files: `src/toolkit/analyze/explain.ts` (edge walk ~:98), NEW `tests/unit/toolkit/analyze/explain-junctions.test.ts`.
Sketch: extend the edge walk to junction nodes using the `wiresOf` pattern already correct in `analyze/structural.ts:63-67`; entrypoint/sink computation includes junction-mediated reachability. Safety: read-only analysis.
Tests: junction-bearing fixture (canonical e2spag001 tab with `e2n12`) — entrypoints/sinks/edges correct through the junction; inverse regression: junction-fed chain no longer reported as a disconnected sink.

**WSB-8 (NEW) — Stop stamping canvas fields on config nodes (e1#9 root cause)** — Size S, depends: none. **[Amended: gates minor — ledger orphan assigned; turns REND-3's exclusion heuristic into belt-and-braces]**
Files: `add-node.ts` (creation path that stamps x/y/z/wires), compile emit path for config-shaped nodes, unit + integration regression.
Sketch: a node detected as config-shaped (no inputs/outputs semantics, referenced-by-id usage, or `isConfigNode`) is created without canvas fields; compile never emits junk x/y/z/wires for config entries. Safety: changes staged bytes for NEW config nodes only — pinned by a recompile-of-existing-flows byte-identity test (adopted flows untouched).
Tests: staged mqtt-broker carries no x/y/z/wires; decompile→compile of the canonical e1 fixture (which contains the historical junk) stays byte-identical (no retroactive scrubbing).

### Workstream C — Layout (R1/R2)

**LAYO-1 — Engine hotfix: junctions as participants, no-crash ELK, center write-back, honest autoEngine (F10, SD5)** — Size S, depends: none.
Files: `elk.ts:74-128`, `dagre.ts:36-100`, `layout/index.ts:33-49`, arbitraries, unit/property tests.
Sketch: register junctions (10×10) in both graphs + shared `applyPositions` write-back (kills the ELK JsonImportException and dagre stranding); fix both write-backs to store CENTER (dagre: delete the top-left subtraction; ELK: `+w/2,+h/2`) — the engines' unreported convention bug, a sibling of F2; delete per-node `clampToBounds`, replace with whole-tab translate + `onDiagnostic` width-overflow callback; autoEngine uses `getOutputPortCount` so rules-only switches count. Safety: zero production call sites at HEAD (verified) — test-substrate only.
Tests: junction-bearing tab completes both engines (E4 crash regression pin); center-convention exact values; 50-node chain no longer piles at x=2400; determinism property over both engines with junction-bearing arbitraries; expected snapshot churn reviewed by hand.

**LAYO-2 — R1 vocabulary, descoped: `lane` + `headerFor` + optional position** — Size M (was M, now smaller), depends: shared `src/toolkit/lanes.ts` (contract #3). **Safety-invariant: test-first (touches compile).**
**[Amended: feasibility major — `stage`/`role` persistence deferred until a consumer exists (no other work item reads them; plan_flow can speak stage names without flows.json persistence); `fallbackPlacement` shrinks from a Kahn mini-engine to a trivial deterministic row-stack reusing D-4's placement helper — no third placement algorithm.]**
Files: `types.ts` (NodeSpec.lane?, position?; GroupSpec.lane?; CommentSpec.headerFor?), `compile.ts` (`_authoringLane`, `_authoringHeaderFor` emitted ONLY when explicitly set), `decompile.ts`, `layout/placement.ts` (reuse), property + unit tests.
Sketch: spread-conditional emit so unannotated flows compile byte-identical (property-pinned). `headerFor` stores the group's authoring key; header ≠ membership (no `g`). Position fallback: D-4's left-margin row placement + `compile/position-defaulted` info diagnostic. **Decompile is parse-or-passthrough: a field is structural-stripped and mapped ONLY when it parses as a valid enum/reference; otherwise it stays in passthrough byte-for-byte — with an invalid-value round-trip property test (`_authoringLane:'bogus'` round-trips verbatim)** **[Amended: safety minor — protects foreign-flow byte-identical adopt-then-noop]**.
Tests (first, passing at HEAD pre-change): unannotated byte-identity; annotated `compile(decompile(compile))` identity; zero-coordinate spec compiles with info diagnostics (R1 acceptance verbatim); headerFor comment carries no `g`; invalid-enum passthrough; all-positions-present specs compile identically before/after.

**LAYO-3 — Lane derivation + section partitioning (pure modules)** — Size S, depends: contract #3 module; parallel with LAYO-2.
Files: `src/toolkit/lanes.ts` (the ONE module — Lane, LANE_NAMES, LANE_ORDER, LANE_GAP=120, closure algorithm, TabSpec + FlowsJson adapters) **[Amended: feasibility blocker — D-2 imports this same module; no `lint/lanes.ts` fork]**, NEW `layout/sections.ts`, unit + property tests.
Sketch: lane resolution order: explicit → group lane → error-closure BFS from seed types (**default: catch/complete seed `error`; status seeds `indicate` — open question #1, decide before any lane test is written**) with conservative shared-sink demotion (reachable from any non-error root stays main); junctions inherit upstream majority. Sections = weakly-connected components, ordered by declared order else min declaration index; groups don't define sections; header association via `headerFor` else the layout-time-only prefix/x-overlap heuristic for foreign flows. Both functions total.
Tests: catch-chain → error; dual-fed node stays main; explicit override; junction inheritance; e2-before 'DECIDE' header heuristic; disconnected pipelines in declaration order; totality + determinism properties.

**LAYO-4 — Two-level layout engine (epic, three slices + kill-switch)** — Size L→epic, depends: LAYO-1..3, REND-2 (GeometryProvider).
Files: REWRITE `elk.ts`, NEW `two-level.ts`, `stack.ts`, `index.ts` entry points + barrel export (F10), `bounds.ts`, tests.
Sketch: Slice A — compound+ports ELK core: layered/RIGHT/randomSeed=1/NODES_AND_EDGES/INCLUDE_CHILDREN/BRANDES_KOEPF; groups (nested via parentKey) as padded compounds; per-node FIXED_ORDER WEST/EAST ports with port index = rule order → affirmative-on-top BY CONSTRUCTION; dims via the GeometryProvider (no 120px hardcodes — grep-guard test); junctions 10×10 waypoints; port-true edges. **Kill-switch decision point after slice A on the e1 fixture (PR #2267 precedent) — if not visually hand-arranged-class, stop; D4 keeps layout opt-in** **[Amended: feasibility major]**. Slice B — per-section, per-lane graphs stacked in LANE_ORDER with **LANE_GAP = 120 imported from `src/toolkit/lanes.ts` — the same constant D-5 teaches and D-7 asserts** **[Amended: gates minor — taught vs shipped reconciled]**; section stacking in declared order; header comments above groups, excluded from group boxes, included in stacking extents; group geometry from compound bounds (top-left, grid-snapped); post-pass grid-snap + translate, no per-node clamping; `LayoutReport` diagnostics (`layout/width-overflow`, `layout/cross-lane-wire`, `layout/group-spans-lanes` — rule names frozen at slice-B merge). Slice C — `layoutTabs(spec)` and `layoutFlowsJson(flows)` (= decompile → layout → compile(prior)); dagre demoted to legacy fallback (open question, default: demote in DESIGN.md). Errors: never throws on well-formed input; ELK failure → `layout/engine-error` diagnostic, tab untouched.
Tests: switch out0-target strictly above out1-target; nested compound containment; junction no-crash at the new layer; ≥240px node gets ≥240 width; error-lane-below (max main center-y < min error center-y); six-header fixture (no pile at origin); pairwise-disjoint containing groups; declaration-order stacking; mod-20 positions; min-extent ≥40; relayout fixed-point property; `compile(layout output)` produces zero lint errors property.

**LAYO-5 — E4 fixture acceptance suite** — Size S, depends: LAYO-4.
Files: canonical fixture dir (see EVAL-3), NEW `tests/integration/layout-e4-fixtures.test.ts`, `layout-metrics.ts` (port of e4-probe flowMetrics), NEW `scripts/layout-bench.mjs`.
Sketch/tests: drive `layoutFlowsJson` on both canonical fixtures and assert R2 literally — e1: zero throw; error-closure nodes strictly below main; switch `3865da1cf3` out0 above out1; six headers above their groups, none co-located, none at (0,0); groups disjoint + containing; backwardWires 0. e2: zero throw with junction `e2n12` (the verbatim crash case); junction repositioned between neighbors; backward 0 + crossings 0; sibling-tab byte-isolation under scope. **Plus: (a) affirmative-on-top asserted on `e2n05` (verified: switch outputs:2/rules:2 — the planner's omission, critic right); (b) a recorded abstention test asserting `deriveLanes(e2spag001)` yields zero error-lane nodes — documenting that R2's "error-lane-below on both fixtures" reduces to e1-only because the spaghetti fixture has no error sources (verified: zero catch/status/complete)** **[Amended: gates minor]**; **(c) min error-lane y − max main-lane y ≥ LANE_GAP on the relaid e1 (taught number = shipped number)** **[Amended: gates minor]**. Diff-based safety pin: positions are the ONLY field class that differs; every pre-existing id survives. Properties asserted, coordinate snapshots forbidden in this file.

**LAYO-6 — `layout_flow` MCP tool (opt-in, staged)** — Size M, depends: LAYO-4, LAYO-5; REND-4 for staged-render review.
Files: NEW `layout-flow.ts`, `toolsets.ts` (NEW `layout` toolset, `default_enabled:false`, flip condition = S6 pass recorded in the description), index/docs/tests. **Async-op widening removed from scope (owned by WSB-5-PR1, already landed)** **[Amended: safety major]**.
Sketch: input `{tab_id, scope?, lane_hints? (not persisted), section_order?, pinned? (verbatim positions; laid section translates below pinned bbox — honest v1 of D3), engine? ('two_level'|'legacy_dagre'), dry_run?}`; dry_run computes report without staging; otherwise one staged change via the standard pipeline. Unknown tab/keys → loud enumerated `ValidationFailedError` (no silent drops — the e1 158-call lesson); width-overflow = warning, never blocks. Safety: zero reorder of the pipeline sequence; layout-only diff pinned.
Tests: validation; dry_run leaves slot empty; pinned byte-identical; integration: one staged change, diff shows only positions/group geometry (added/removed/wires all 0); single-slot respected; deploy→re-layout→zero-diff (idempotent loop); `_authoringKey` identity pre/post.

### Workstream D — Lint & guidance (R4/R6)

**D-1 — layout-lint core: geometric rules + sole owner of flows-lint geometry (absorbs REND-6)** — Size M, depends: REND-2 (GeometryProvider). **[Amended: blocker — single ownership; all rule-id/severity decisions recorded here once]**
Files: NEW `src/toolkit/lint/layout-lint.ts`, NEW `geometry.ts`, `flows-lint.ts` (replaces fixed 120×40 top-left boxes at :17-22/:36-56, extends the :74 group/comment skip), `toolkit/index.ts`, config schema (`LINT_VIEWPORT_WINDOW_WIDTH` default 1920), `_stage-pipeline.ts` options only.
Sketch: `layoutLint(flows, opts): {overall, rules[{rule, score, weight, offenders}], diagnostics}` with `overall = 1 − Σw(1−s)/Σw`. **Geometry comes exclusively from the GeometryProvider (REND-2's `nodeDimensionsFor` for BOTH w and h, plus port anchors) — D-1 contains no port or height formulas of its own; when the provider improves, lint, placement, render, and engine move together** **[Amended: feasibility major — no "three disagreeing geometries" reborn]**. Rules: layout-wire-crossings (w3; renderer's exact bezier flattened K=8, AABB prefilter, shared-port exclusions; O(W²K²) bound documented, <50ms at 250 wires); layout-backward-wires (w3; port-x with 20px tolerance); occlusion via center boxes — **decision: rule ids `bbox-overlap` and `off-canvas` stay STABLE (audit canaries reference them); the new scored rules carry the `layout-*` namespace**; layout-group-overlap (w2, sibling-only); layout-viewport-overflow (w1, INFO — fires on e1-agent's 1560px extent by design); off-canvas extended to groups/comments as **box-not-point** checks. **Severity policy, recorded once: node off-canvas stays `error` (HEAD behavior, verified); everything newly covered (groups/comments, all layout-\* rules) is warning/info — the stage gate at `_stage-pipeline.ts:109-114` (verified: lint errors throw) can never newly block; promotion is a tracked open question.** Safety: read-only; pinned by "previously-staging-clean fixture still stages".
Tests: geometry truth table (X-cross 1; parallel 0; fan-out 0; convergence 0); port anchors per provider; **REND-6's named cases folded in: e3 planted-defect pair still flagged; wide nodes 140px apart caught / narrow 110px cleared; group-half-off-canvas warning; comment at negative center warning**; center-convention regression; determinism + score∈[0,1] + never-error properties; 200-node perf smoke; stage-still-stages regression.

**D-2 — Semantic rules: error-lane-below, stage order, affirmative-on-top, header-presence** — Size M, depends: D-1 + shared `src/toolkit/lanes.ts` (contract #3; **no `lint/lanes.ts` fork — the FlowsJson adapter lives in the shared module**).
Sketch: error-lane-below (w2, MEDIAN-based — verified necessary: e1-agent errMedY 560 > mainMedY 260 passes while min/max would false-fail); stage-order via inter-group DAG centroids (w2, abstains ungrouped); affirmative-on-top (w1, port-0 mean target-y ≤ every port-k); header-presence (w1, groups ≥3 members need name or header comment). Annotation reader seam reads `_authoringLane` when LAYO-2 lands; topology inference always available. All abstain rather than guess.
Tests: closure marks catch→fn→debug error; dual-fed stays main; junction traversal; numeric median pins against e1-agent geometry; per-rule fail fixtures; abstentions.

**D-3 — Wire layout lint into read surface + layout-scores nudge** — Size M, depends: D-1, D-2. Lands v1.5.0; output-schema growth is the priced integration-migration bill, additive.
Sketch: validate_flow/validate_all_flows swap to `lintFlows({layout:true})` with a `layout` output block (offenders capped at 10/rule + true count); analyze tools gain per-tab `layout`; CLI gains `layout` key + `--no-layout`, exit codes unchanged; nudge `layout-scores` (author-tier except plan_flow, per-stagedHash cache, silent ≥0.95, ≤300 chars, defensive) through the existing `_guidance` channel; `_stage-pipeline.ts:103` gains `layout:true` (in merge-order position after REND-8). Safety: no error-severity layout diagnostics → stage gate cannot newly block; nudges are post-handler and contractually defensive.
Tests: response shapes; nudge fires/caches/silences/never-breaks-tool; CLI exit-code regression; stage-then-inspect integration with deploy still permitted.

**D-4 — Fix F12: width-aware placeRightOf + left-margin row fallback** — Size S, depends: D-1's provider (or REND-2 directly). **Safety-invariant: test-first.**
Sketch: `placeRightOf(source, {sourceWidth, newWidth, gap=60, occupied})` → step `max(140, ceilToGrid(srcW/2 + gap + newW/2))` (inside the taught 140-220px pitch); deterministic +60 y-bump collision avoidance over `occupied`; callers (add-node:81, add-debug-node:69, \_spawn) supply provider widths. No-position fallback: replace the x=160 mid-canvas pile with left-margin new-row placement (x = min existing x clamped ≥120, y = content maxY + 80, snapped) — this helper is what LAYO-2's fallback reuses. Safety: placement is pre-compile spec data; pure/deterministic; explicit-position calls byte-identical (idempotency canary); default-placement change flagged in CHANGELOG.
Tests: step math narrow/wide; property — 12-node chained add_node with random labels → zero D-1 occlusion offenders (pins "the one spatial decision FlowOtter makes, it makes wrong"); fallback rows; explicit-position byte-identity.

**D-5 — R6 conventions with numbers: SERVER_INSTRUCTIONS (sole owner), catalog category, prompts** — Size S, depends: none hard (catalog cross-check activates with D-1/D-2).
Sketch: **D-5 is the single owner of the SERVER_INSTRUCTIONS rewrite — one PR budgets ALL of: the ~290-char layout-numbers line (20px grid; 140-220px pitch; error lane ≥120px below — the LANE_GAP constant; port-0 on top; ≤~1420px viewport; validate_flow returns layout scores), REND-5's render_flow_png mention, and WSB-5's stage_changes phase-3 sentence, funded by the named trims (SPECIALISTS examples, CREDENTIALS/DASHBOARDS compression); verified 1902/2000 at HEAD, ≤2000 pinned by instructions.test.ts** **[Amended: feasibility major — triple-contention resolved]**. Catalog: `layout_conventions` category, EIGHT entries 1:1 with audit criteria, each naming its `lint_rule`; bidirectional completeness test (catalog ↔ registered rule ids cannot drift). Prompts: new_flow step 5 + review_my_flow layout-scores step.
Tests: length ≤2000 + token assertions ('20px', '140-220', 'BELOW', 'port 0', '1420', '120'); deliberate snapshot re-bless; catalog cross-check; prompt tokens.

**D-6 — plan_flow spatial scaffold + PlanRecord v2** — Size M, depends: D-2 (shared lanes module). Phase 3.
Sketch: pure `buildSpatialScaffold(stages)` → grid 20, pitch 200, viewport {1920, 1420}, per-stage x_centers, lane y-bands with the error band ≥ LANE_GAP below main; stages gain optional `lane`; PlanRecord schema_version 1|2, readPlan accepts v1, writePlan emits v2; `layout_strategy` stays 'manual' until LAYO-6's toolset flips. Plan.json is a sidecar — no staging-path impact.
Tests: scaffold determinism, band math, monotonic x_centers, v1 fixture back-compat.

**D-7 — Acceptance capstone: separation test + S7 conventions-in-band probe** — Size S, depends: D-1, D-2, D-3, D-5, D-6, **and REND-2** **[Amended: safety minor — numeric pins calibrate once against the editor-true provider, with a provider-identity assertion that makes any future swap loud]**. Phase 3.
Sketch: canonical fixtures (e1-agent, e1-dagre, e1-elk, spag-raw — engine derivatives regenerated by script where deterministic). Separation asserts score-ORDERING + per-rule expectations, never zero-diagnostics — encoding the honesty caveat that editor-true widths correctly find 3 real overlaps on the audit's best fixture: score(e1-agent) > score(x) + margin for each x; spag-raw backward==8, crossings ≥1; engine outputs: group-overlap ≥2, comment-pile 6, off-canvas groups 2, lane inversion fires; e1-agent occlusion ≤3 at ≤warning (F11 true positives pinned as expected). **The margin (0.15), the per-rule expectations, and the ≤3 carve-out are PRE-REGISTERED in EVAL-3's thresholds.json/PROTOCOL.md and read by this test — the R4 gate definition is owned by the gates workstream, not the lint author** **[Amended: gates major]**. S7 static probe: every one of the eight criteria + every numeric constant appears in ≥1 agent-visible channel (instructions/catalog/plan_flow output); anti-return guard asserting the old identical-scoring degeneracy (F8) is gone.

### Workstream E — Gates, eval harness, rollout

**EVAL-1 — Promote the MCP eval driver: budgets, sections, expectations, harness-fault fixes** — Size M, depends: none.
Sketch: `scripts/eval/driver.mjs` (from the gitignored original) + `budget.mjs` with counters {mcp_calls, failed, exec_steps, total_invocations, deploy_confirmations, elicitation_declines, force_uses, force_takeover_uses, oob_mutations} and `checkBudget`; steps-file schema v2 (sections/budgets/expect/mutates/elicitation accept|decline); $PREV-poisoning hard-error (pins the 79-call-cascade class) + EPIPE guard; `FLOW_OTTER_CMD` env. **Plus, pulled forward from EVAL-5: the shared wiring-map/idempotence comparator util (so EVAL-6's canaries in Phase 1 don't depend on Phase-2 EVAL-5)** and the **budget-definition glossary in EVALUATION.md, including the counting boundary: "budgeted section starts at the first author-tier call and ends at deploy; setup/target/read-discovery calls before it are unbudgeted" — cited by WSB-5's vitest acceptance** **[Amended: gates minor]**. **Steps-file lint: any `stage_spec` payload containing position fields in a section flagged layout-computed fails the run** **[Amended: gates blocker — anti-gaming]**. Safety: read-only harness.
Tests: every counting rule pinned; boundary cases; $PREV-poisoning abort; driver integration on the compose stack (pass, budget-violation exit 1, expect machinery).

**EVAL-2 — S5 gate re-spec: total-invocation budget + live-editor fidelity** — Size M, depends: EVAL-1, REND-5, **REND-8** (explicit), REND-7 (comparator).
Sketch: S5 = ≤6 TOTAL invocations rendering STAGED state. **Canonical steps file re-authored to be structurally passable (verified: the original shape throws on the single-slot guard): unbudgeted setup (seed defect, deploy w/ confirm); budgeted loop {max_total_invocations:6, max_failed:0}: move_node(1) [REND-8 returns after_png path] → exec Read after_png(2) → discard_staged_change(3) → move_node adjust(4) → exec Read after_png(5); unbudgeted verify: deploy (1 confirmation) + fidelity check** **[Amended: gates blocker]**. Fidelity: editor dump via the **shared `scripts/eval/cdp.mjs`** (no playwright/puppeteer devDep — size claim corrected: puppeteer-core would be ~5-8MB, but zero-new-deps wins; it remains the documented fallback) and **REND-7's single comparator library** against `renderGeometry` output **[Amended: contract #1 + harness unification]**. `--record` mode until REND-5/8 land; gate = twice-consecutive + the Phase-1 live session. The comparator committed as a red test at HEAD (F2 anchors) that REND-3 turns green.
Tests: comparator covered in REND-7; steps-file budget block IS the F1/e3 regression; env-gated dump smoke.

**EVAL-3 — S6 pre-registration: corpus, manifest, both legs, frozen thresholds** — Size M, depends: none for pre-registration; leg-B specs ride LAYO-2. Phase 2 (must precede the first scored run, Phase 3).
Sketch: **One canonical committed fixture directory — `tests/fixtures/audit-2026-06-10/` — with hash-pinned files and a tiny loader; ALL work items (REND-3, LAYO-5, D-7, EVAL-3 corpus, EVAL-5 seed) reference it; engine-output derivatives are regenerated by script, not committed copies** **[Amended: feasibility major — fixture proliferation across five paths eliminated; verified sterile: localhost/mosquitto only]**. Manifest entries with ≥1 operator-semantics criterion each; charter members = the two audit fixtures; community flows fetch-by-manifest with sha256 pins (licensing open question). PROTOCOL.md pre-registers legs (A stripped, B zero-coordinate annotated spec), blinded seeded A/B, judges. thresholds.json frozen — **freeze is enforced by content hash: the sha256 of thresholds.json and PROTOCOL.md is recorded in PROTOCOL.md itself and in the DESIGN.md ratification entry; EVAL-4 verifies content hash, refusing `--scored` on mismatch unless invoked with `--superseded-thresholds <new-sha>`, which prints both SHAs + the commit list in the gate verdict** **[Amended: gates major — committed edits no longer pass a dirtiness-only check]**. **D-7's R4 separation criteria live here as pre-registered values.** **Manifest test asserts no spec under `eval/benchmark/specs/` contains a position key on any node (junction exemption decided and recorded)** **[Amended: gates blocker]**.
Tests: manifest schema validation; corpus loads through FlowsJsonSchema; sha256-mismatch hard fail; zero-coordinate assertion.

**EVAL-4 — S6 scoring runner + blinded judging packets** — Size M, depends: EVAL-3; gate-pass needs LAYO-4, D-1/D-2, REND-5. Phase 3 scored.
Sketch: port e4-probe `flowMetrics`/`stripPositions` as pure modules; runner per manifest×leg through the engine adapter (one seam — survives LAYO API churn), recording BOTH raw metrics and layout_lint scores with {version, commit, engine, thresholds_sha}; blind-pack with seeded side assignment, answer key gitignored; scorer computes not_worse_rate/lint deltas/semantics pass vs hash-verified thresholds. Expected at HEAD: the crash counter >0 fails until LAYO lands; F8's identical-scoring degeneracy must disappear when D-3 wires lint.
Tests: metrics math pins; threshold boundary cases; blind-pack determinism; `--scored` refusal on hash mismatch.

**EVAL-5 — Audit-replay regression suite (e1/e2 scenarios, phase-gated budgets, safety post-conditions)** — Size M, depends: EVAL-1; e2 pass gated on WSB-5; e1-phase2 on stage_spec (Phase 3). Phase 2; **merges before or with WSB-5's first staging PR**.
Sketch: `replay.mjs` with fresh `ENVIRONMENT_NAME` per run; budgets.json: e2.phase1 {max_mcp_calls:5, max_deploy_confirmations:1, max_failed:0, max_force:0, max_oob:0}; e1.phase1 {≤30/≤3/0-failed} (proposal, sanity-passed once WSB-5 granularity is final); e1.phase2 {max_authoring_calls:3, 1 confirmation} — **e1-phase2 steps file is run under EVAL-1's position-field lint (zero-coordinate path enforced)** **[Amended: gates blocker]**. S5 delegates to EVAL-2's one canonical steps file. Safety post-conditions on EVERY run: deploys == confirmations with non-null snapshots; force/force_takeover == 0; e2 wiring-map byte-identity; scenario-level idempotence via canonicalHash (pins byte-identical compile + `_authoringKey` preservation through the batch path). Steps authored against WSB-5's published op-schemas in the same PR series (contract #4); `--expect-fail` record mode until then.
Tests: wiring-map canonicalization unit; the scenarios ARE the phase exits.

**EVAL-6 — Per-batch verification protocol + S1/S4 canaries + AUDIT-RERUN.md** — Size S, depends: EVAL-1. Phase 1 (canary files gate the Phase-1 exit; AUDIT-RERUN.md may trail).
Sketch: EVALUATION.md codifies `npm run eval:canary` after any fix batch + the twice-consecutive rule + committed-numbers-not-author's-head rule. s4-steps.json: stage → mutates:true OOB POST → deploy expect drift error → rollback → byte-compare restore → elicitation-decline leg (verify the SDK ^1.x client elicitation API before authoring) → READ_ONLY_MODE leg. s1-steps.json: README Tab-1 loop, budget-recorded, idempotency post-condition via EVAL-1's comparator. AUDIT-RERUN.md: the FULLY FIXED protocol with the **mechanical friction anchors from §1** and the **explicit zero-coordinate disqualification rule** **[Amended: gates majors]**; **live-session confirmation is BINDING for S5/S7-class gate declarations (scripted run = standing regression; live run = ergonomic proof), recorded in run files** **[Amended: gates major]**.
Tests: s4 must PASS at HEAD (the spine holds — these are pinned credits); any future failure blocks everything.

**EVAL-7 — Ratify the rollout spine in DESIGN.md + sanitized audit record** — Size S, depends: none — **lands first**.
Sketch: commit the sanitized audit report under `docs/audits/2026-06-10-layout-audit.md` — **run `privacy:scan` (which honors `~/.flow-otter/privacy-patterns.txt`) plus one manual read; the "strip /Users/… prefixes" instruction is dropped (verified: the artifact inventory already uses relative paths)** **[Amended: feasibility minor]**; publish-full-vs-condensed is open question #10. DESIGN.md amendment **references work-item ids exactly as in §2's phase table — and corrects the mischaracterization: the stage-over-stage refusal already exists at HEAD (verified `_stage-pipeline.ts:72-82`); B's Phase-1 guards are WSB-1 (transport serialization), WSB-3 (no-op refusal + hash-equal auto-clear), WSB-6 (casing/agent_id/alias)** **[Amended: safety minor]**; D-3's schema growth explicitly versioned v1.5.0 additive; the Phase-0 live half-day spike is **restored as the binding live-session exit requirement, not superseded** **[Amended: gates major]**; stage_spec recorded as moved to Phase 3 (fixes §5.6); the five frozen contracts named; recorded deferral entries for e1#13 (debug buffer laziness — subscribe-on-target, deferred with owner-on-record) and any other triaged-but-deferred ledger items. **Also fixes the stale `EVALUATION.md` claim that `files` ships `docs/`** (verified false) **[Amended: feasibility minor]**.
Tests: privacy-scan green; supersession records reviewed against the doc's header convention.

---

## 4. Finding coverage matrix

No unpinned findings. WSB-7 was added expressly to close SD7 (the one gap all three critics' matrix check confirmed).

| Finding                                         | Work item(s)           | Named regression pin                                                                                                                         |
| ----------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 — no image channel / S5 loophole             | REND-5, REND-8, EVAL-2 | `render-flow-png` unit+integration (magic bytes, RasterizerUnavailableError hard-fail); `s5-steps.json` budget block (≤6 total invocations)  |
| F2 — top-left vs center anchors                 | REND-3, EVAL-2         | svg assertions (d) (e) (f); fidelity comparator committed red at HEAD, green after REND-3                                                    |
| F3 — phantom `passthrough` port counts          | REND-3                 | assertion (a): e1 switch `3865da1cf3` renders exactly 2 output ports                                                                         |
| F4 — conventions taught nowhere                 | D-5, D-7               | `instructions.test.ts` token assertions ('20px','140-220','BELOW','port 0','1420','120'); `conventions-in-band.test.ts` eight-criteria probe |
| F5 — ~6 calls/node reorganization cost          | WSB-5, EVAL-5          | `e2-reorg-budget.test.ts` (≤5+1, glossary-cited counting); `budgets.json` e2.phase1                                                          |
| F6 — junctions dropped/mis-rendered             | REND-3                 | assertion (c): junction `<circle>` + outgoing wire path                                                                                      |
| F7 — render always reads runtime                | REND-4                 | staged-vs-runtime render unit pair + empty-slot diagnostics test                                                                             |
| F8 — lint scores everything identically         | D-1/D-2/D-3, D-7       | separation suite + anti-return guard (engine outputs no longer 0-diagnostic)                                                                 |
| F9 — width model diverges from editor           | REND-2, REND-7         | `metrics-editor-truth.test.ts` ±2px vs fixture; env-gated live fidelity check                                                                |
| F10 — layout engines crash/strand/unexported    | LAYO-1, LAYO-4, LAYO-5 | junction no-crash pins (both engines, both layers); 120px grep-guard; barrel-export via `layoutFlowsJson` e2e                                |
| F11 — measured +37-53px width deltas            | REND-2                 | named cases exact: 'Parse reading' 160 not 107; 'Debounce' 220 not 163                                                                       |
| F12 — placeRightOf overlap-by-construction      | D-4                    | 12-node chain property → zero occlusion offenders; fallback row tests                                                                        |
| SD1 — cross-tab deriveId id-steal               | WSB-2                  | `compile-cross-tab-id-steal.test.ts` + extended arbitraries property                                                                         |
| SD2 — diagnostics dropped at transport          | WSB-1                  | `tool-error.test.ts` + integration ("error(s)" never without cause)                                                                          |
| SD3 — junction ops silent no-op/stage           | WSB-3, WSB-4           | `stage-noop-guard.test.ts` (slot stays empty); junction move/update/remove op suites                                                         |
| SD4 — comments/groups write-only                | WSB-4                  | update/remove group+comment op suites; e2 lifecycle integration                                                                              |
| SD5 — ELK JsonImportException / dagre stranding | LAYO-1, LAYO-5         | E4 crash regression fixtures; `e2n12` zero-throw at both layers                                                                              |
| SD6 — process-pinned staging + casing mismatch  | WSB-6, WSB-3           | dual-emit casing test; staged_hash→deploy without rename; foreign-agent hash-equal auto-clear test                                           |
| SD7 — explain_flow ignores junction edges       | **WSB-7 (new)**        | `explain-junctions.test.ts` on the canonical e2spag001/`e2n12` fixture                                                                       |

Ledger orphans assigned: e1#9 → WSB-8 (root cause) + REND-3 (belt-and-braces); e1#10 → WSB-5 resolution table; e3#2 → WSB-6 alias + nudge; e1#13 → recorded deferral in EVAL-7.

---

## 5. Cut list (if capacity halves)

**Ships:** WSB-1, WSB-2, WSB-3, WSB-6, WSB-7 (S-size, required to keep the matrix fully pinned), WSB-5 (batching is the audit's largest cost multiplier) + the junction/comment-addressing half of WSB-4; REND-1..5; D-4, D-5; LAYO-1; EVAL-1, EVAL-2 (record→gate), EVAL-7.

**Defers (with recorded deferral entries per the supersession convention):** LAYO-2..6 wholesale (engine stays dormant-but-uncrashing via LAYO-1; D4 already keeps it opt-in), D-1/D-2/D-3 (or minimally backward-wires + group-overlap only), D-6, D-7, REND-7, REND-8, WSB-4's group-tools half, WSB-8, EVAL-3/4/5/6.

**Degraded definition of done:** S5 budget rises to **≤7 total invocations with a recorded supersession** (explicit `render_flow_png against:'staged'` calls + mandatory discard, since REND-8's in-stage renders are cut — the gates-blocker's sanctioned alternative); e2 budget asserted by WSB-5's vitest acceptance only (no driver replay); fidelity asserted by REND-2's fixture test only (no live comparator); S6 / FULLY FIXED / friction scores deferred entirely — the release claim downgrades from "fully fixed" to "visual channel truthful + batch staging shipped," and DESIGN.md says so explicitly. This preserves the audit's own priority order (R3 + R5 first, R6 cheap, R1/R2/R4 gated).

---

## 6. Open questions (must answer before the phase starts)

| #   | Question                                                                  | Phase                                      | Owner                 | Default if unanswered                                                          |
| --- | ------------------------------------------------------------------------- | ------------------------------------------ | --------------------- | ------------------------------------------------------------------------------ |
| 1   | Status-node lane: `indicate` or `error` closure seed?                     | 2 (before any D-2/LAYO-3 lane test)        | User                  | `status → indicate`; catch/complete → error (matches semantics + e1 reference) |
| 2   | PNG font: bundle OFL subset (~200KB tarball delta) vs `loadSystemFonts`?  | 1 (before REND-5 tests)                    | User (install weight) | Bundle, base64-embedded TS module                                              |
| 3   | SERVER_INSTRUCTIONS trims (SPECIALISTS/CREDENTIALS/DASHBOARDS cuts)?      | 1 (before D-5 PR)                          | User                  | D-5's named cuts as specced                                                    |
| 4   | WSB-3 auto-clear of foreign-agent byte-identical stale stages?            | 1                                          | User                  | Clear (information-lossless by construction; kills the restart friction)       |
| 5   | Publish the full candid audit report vs condensed summary?                | 1 (EVAL-7 lands first)                     | User                  | Full sanitized report under `docs/audits/`                                     |
| 6   | S6 judge identity (2 judges + tie-break)?                                 | 2 (must be in PROTOCOL.md at EVAL-3 merge) | User                  | Maintainer + fresh no-context agent session; third judge tie-break             |
| 7   | R4 separation margin 0.15 + e1-agent ≤3-occlusion carve-out ratification? | 2 (pre-registered in thresholds.json)      | User (R7 owner)       | As specced, calibrated once against REND-2's provider                          |
| 8   | `add_junction` op in WSB-4/WSB-5 union?                                   | 2                                          | Implementing agent    | Include (minimal op; batch union + engine need it)                             |
| 9   | LANE_ORDER (`main,indicate,error`) + LANE_GAP=120 ratification            | 3 (before LAYO-4 slice B freezes)          | User                  | As specced (matches e1 human reference; gap = taught number)                   |
| 10  | Retire dagre to "simple-chain fallback" in DESIGN.md?                     | 3 (LAYO-4 ship)                            | User                  | Demote with supersession record; keep `legacy_dagre` escape hatch              |

All other inherited open questions (op cap, casing-removal timing, refit defaulting, pinned-v1 semantics, phantom-config heuristic, image downscale clamp, editor width cap — answered empirically by REND-1, SVG-as-image, community licensing, e1 interim budget) are deferrable and tracked in the work items.

---

## 7. Risk register (top 8)

| #   | Risk                                                                                                                                   | Trigger                                                             | Mitigation                                                                                                                                                                                                       | Bites in           |
| --- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| 1   | **LAYO-4 is the PR-#2267 graveyard** — ELK compound + FIXED_ORDER + stacking never looks hand-arranged                                 | Slice A output visually wrong on the e1 fixture                     | Kill-switch decision point after slice A; D4 keeps the tool opt-in (default-OFF toolset) so the walk-back strands nothing; S6 thresholds pre-frozen so "good enough" can't be redefined                          | Phase 3            |
| 2   | **`_stage-pipeline.ts` pileup** — five items, one safety choke point                                                                   | Out-of-order merges; REND-8 patching code WSB-5-PR1 relocates       | Published binding merge order (§2); enrichment lives inside `compileValidateAndStage`; every later PR re-runs earlier invariant pins                                                                             | Phases 1-2         |
| 3   | **Snapshot re-bless masks new renderer bugs** (the function_three_outputs failure mode)                                                | REND-3's mass re-bless of all 5 SVG snapshots                       | Re-bless protocol: snapshots only change alongside an assertion test naming the geometry; assertion tests (a)-(f) survive re-blessing                                                                            | Phase 1            |
| 4   | **WSB-2 blast radius** — every compile passes through deriveId                                                                         | Ordering-sensitive two-pass restructure regression                  | Test-first deterministic repros + extended arbitraries; elevated `VITEST_PROP_RUNS`; five named property/integration pins must stay green; no deployed flow depends on the buggy steal (it was refused by lint)  | Phase 2            |
| 5   | **Phase 1 overload for a solo maintainer** (~15 items)                                                                                 | Schedule slip; rushed safety-adjacent reviews                       | REND-7-live and AUDIT-RERUN.md designated trailers; REND-4/WSB-1/WSB-6 are independent quick wins; the §5 cut list is pre-agreed, not improvised                                                                 | Phase 1            |
| 6   | **Gate gaming / threshold drift** — committed loosened thresholds, hand-placed specs, sympathetic judges                               | Any scored run after engine work begins                             | sha256 content-hash freeze with loud `--superseded-thresholds` path; mechanical zero-coordinate assertions; friction derived from the budget account; blinded seeded A/B; twice-consecutive + live-session rules | Phases 2-4         |
| 7   | **Metrics drift across Node-RED versions / AFM-vs-browser mismatch** near 20px snap boundaries                                         | 5.0 fixture differs from 4.1; ±2px failures at boundaries           | Cross-version diff test fails loudly with a table; compile pins the 4.1 profile permanently (purity preserved); calibrated scale-factor fallback; fidelity re-run on every Node-RED minor                        | Phase 1, recurring |
| 8   | **Test-migration bill** — D-3 output-schema growth + REND-2 autofit geometry shift + WSB-4 silent-false test migration across 777u/85i | Concurrent PRs asserting on compiled geometry or tool output shapes | Bills priced inside each item; D-3 schemas additive and versioned v1.5.0; REND-2/REND-3 scheduled away from concurrent geometry-asserting PRs; WSB-4 owns the op-contract test migration explicitly              | Phase 2            |

---

## 8. Workstream F — Node-RED 4.1/5.0 feature completeness (NR5, supplemental)

Source: dedicated planning agent, 2026-06-10 (full work-item JSON: `eval-results/2026-06-10-layout-audit/NR5-workitems.json` — local audit artifact, gitignored). Headline: the 20-entry capability matrix is **dead coverage** — only `health_check` display consumes it; and there is **no config-node authoring path** (`add_node` is tab-scoped), blocking mqtt-broker/tls coverage.

**Phase mapping:** NR5-1 (thread capabilities into validate/author pipelines — foundation), NR5-13 (formalize the 5.0 compose leg), NR5-4-link-half ride **Phase 2**. NR5-2/3/5/6/7/8/9/10/11/12/15 + a new author-tier `update_tab` (label/info/env) land in **Phase 3** before the v2.0.0 ship. NR5-14 items are cross-references: junction rendering is REND-3's (done in Phase 1); unknown-installed-type #fee fill and group info-badge glyph land with the Phase-3 renderer touchups; junction layout participation is LAYO-1/4's.

**Dedup adjudications:** NR5-4's junction-edge half is **superseded by WSB-7** (same fix, same file); NR5-4 narrows to static-link + `node.linkcall` virtual edges (`kind:'link'|'linkcall'`). NR5-14's junction items are owned by REND-3/LAYO-1.

**Driver decisions on NR5 open questions:** new `add_config_node` tool in the author toolset (not a tab-less add_node mode); author-tier `update_tab` included in Phase 3; nodeDefaults = strict add_node materialization only (no validation-side merging); MQTT v5/tls flows.json field names verified against core node sources in the running container before schemas lock; RED.util.getSetting exact-version list verified upstream at implementation time; oauthCodeExchange → declared non-goal (NON_GOALS.md entry); explain_flow junction edges hop-by-hop (no collapsing), junctions excluded from entrypoint/sink heuristics.

| Item   | Title                                                                                                           | Size | Phase |
| ------ | --------------------------------------------------------------------------------------------------------------- | ---- | ----- |
| NR5-1  | Thread runtime capabilities into validation/authoring (foundation)                                              | M    | 2     |
| NR5-2  | `version-compat` lint family (feature-on-too-old-target warnings)                                               | M    | 3     |
| NR5-3  | `node.linkcall` implicit edges in analyze/explain + link-resolution warning                                     | M    | 3     |
| NR5-4  | Static-link/linkcall virtual edges in explain (junction half → WSB-7)                                           | M    | 3     |
| NR5-5  | First-class `info` authoring → self-documenting flows (4.1+ doc badges)                                         | M    | 3     |
| NR5-6  | `/settings` nodeDefaults (≥4.1.9) merged into add_node materialization                                          | M    | 3     |
| NR5-7  | Known-issue lint: RED.util.getSetting broken in 5.0.0 sandbox                                                   | S    | 3     |
| NR5-8  | `add_config_node` tool (prereq for broker/tls; credentials stay empty)                                          | M    | 3     |
| NR5-9  | tls-config schema: pfx/env-vars/SNI + http-request SNI notes                                                    | S    | 3     |
| NR5-10 | MQTT v5 properties: in/out fields + mqtt-broker schema                                                          | M    | 3     |
| NR5-11 | Catalog 4.x/5.x sweep (burst, ESM, node:-prefix, JSONata2, GH alerts, linkcall) + `capability` cross-check test | M    | 3     |
| NR5-12 | Subflow per-instance config-node env authoring                                                                  | M    | 3     |
| NR5-13 | Formalize NR 5.0 compose leg + capability-assertion integration test                                            | S    | 2     |
| NR5-14 | Renderer/layout cross-refs: #fee unknown-installed-type, group doc badge                                        | S    | 3     |
| NR5-15 | Ops/docs: adminCorsDefault, credsAlongsideFlows, `capability_requirements` in health_check                      | S    | 3     |
